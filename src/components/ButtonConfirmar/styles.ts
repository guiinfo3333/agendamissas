import styled from 'styled-components';


export const ButtonConfirmarComponent = styled.button`
background-color:var(--secondary);
color:var(--primary);
font-family: 'Noto Sans',sans-serif;
font-size:24px;
border-radius:8px;
height:50px;
position: relative;
width:100%;
`;
